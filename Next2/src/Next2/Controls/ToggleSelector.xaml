<?xml version="1.0" encoding="UTF-8"?>
<StackLayout
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Next2.Controls"
    x:Class="Next2.Controls.ToggleSelector">

    <StackLayout.GestureRecognizers>
        <PanGestureRecognizer PanUpdated="PanGestureRecognizer_PanUpdated"/>
        <TapGestureRecognizer Command="{Binding TapCommand, Source={RelativeSource AncestorType={x:Type controls:ToggleSelector}}}"/>
    </StackLayout.GestureRecognizers>
    
    <Grid
        HorizontalOptions="EndAndExpand"
        RowDefinitions="*"
        ColumnDefinitions="56">

        <Frame
            Grid.Row="0"
            BackgroundColor="{Binding OnColor, Source={RelativeSource AncestorType={x:Type controls:ToggleSelector}}}"
            VerticalOptions="Center"
            HeightRequest="32"
            WidthRequest="56"
            CornerRadius="{OnPlatform Android='20', iOS='15'}"/>

        <Frame
            x:Name="runningFrame"
            HeightRequest="26"
            WidthRequest="26"
            Margin="2, 0"
            BackgroundColor="{Binding ThumbColor, Source={RelativeSource AncestorType={x:Type controls:ToggleSelector}}}"
            HorizontalOptions="Start"
            VerticalOptions="Center"
            CornerRadius="{OnPlatform Android='20', iOS='15'}"/>

    </Grid>

</StackLayout>