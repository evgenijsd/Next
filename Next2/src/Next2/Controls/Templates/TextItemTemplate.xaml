<?xml version="1.0" encoding="UTF-8"?>
<StackLayout 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:templates="clr-namespace:Next2.Controls.Templates"
    x:Class="Next2.Controls.Templates.TextItemTemplate">

    <BoxView
        HeightRequest="{OnPlatform Android=0.25, iOS=1}"
        Color="{DynamicResource TextAndBackgroundColor_i6}"/>

    <StackLayout
        Margin="14, 9"
        HeightRequest="{Binding ItemHeightRequest, Source={RelativeSource AncestorType={x:Type templates:TextItemTemplate}}}"
        Orientation="Horizontal">

        <Label
            FontSize="{Binding FontSize, Source={RelativeSource AncestorType={x:Type templates:TextItemTemplate}}}"
            FontFamily="Barlow-Medium"
            TextColor="{DynamicResource TextAndBackgroundColor_i1}"
            FormattedText="{Binding Text, Source={RelativeSource AncestorType={x:Type templates:TextItemTemplate}}}"
            VerticalTextAlignment="Center"
            HorizontalOptions="FillAndExpand"/>

    </StackLayout>

</StackLayout>