<?xml version="1.0" encoding="UTF-8"?>
<StackLayout
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:templates="clr-namespace:Next2.Controls.Templates"
    x:Class="Next2.Controls.Templates.BonusItemTemplate"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    HorizontalOptions="Fill"
    VerticalOptions="Start"
    Margin="0,7,0,0">

    <VisualStateManager.VisualStateGroups>

        <VisualStateGroupList>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="Selected">
                    <VisualState.Setters>
                        <Setter Property="BackgroundColor" Value="Transparent" />
                        <Setter TargetName="checkBoxImage" Property="ffimageloading:CachedImage.Source" Value="ic_check_box_checked_primary_24x24" />
                        <Setter TargetName="bonusFrame" Property="Frame.BorderColor" Value="{DynamicResource AppColor_i1}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateGroupList>

    </VisualStateManager.VisualStateGroups>

    <Frame
        BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, Path=BackColor}"
        BorderColor="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, Path=BorderBonusColor}"
        CornerRadius="8"
        HeightRequest="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, Path=HeightBonus}"
        x:Name="bonusFrame">

        <Grid
            Grid.Row="0"
            ColumnDefinitions="*,50">

            <ffimageloading:CachedImage
                Source="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, Path=ImagePath}"
                Grid.Column="1"
                HeightRequest="25"
                HorizontalOptions="CenterAndExpand"
                Margin="0,0,15,0"
                VerticalOptions="CenterAndExpand"
                x:Name="checkBoxImage" />

            <Label
                Grid.Column="0"
                HorizontalOptions="FillAndExpand"
                Margin="20,0,0,0"
                VerticalOptions="CenterAndExpand"
                Text="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, Path=Text}"
                LineBreakMode="TailTruncation"
                TextColor="{DynamicResource TextAndBackgroundColor_i1}"
                FontSize="{DynamicResource TSize_i7}"
                FontFamily="Barlow-SemiBold" />

        </Grid>

        <Frame.GestureRecognizers>
            <TapGestureRecognizer
                Command="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, 
                                                        Path=TapSelectCommand}"
                CommandParameter="{Binding Source={RelativeSource AncestorType={x:Type templates:BonusItemTemplate}}, 
                                                        Path=TapSelectCommandParameter}" />
        </Frame.GestureRecognizers>

    </Frame>

</StackLayout>