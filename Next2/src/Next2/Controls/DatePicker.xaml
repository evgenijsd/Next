<?xml version="1.0" encoding="UTF-8" ?>
<Frame
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:local="clr-namespace:Next2"
    xmlns:controls="clr-namespace:Next2.Controls"
    Padding="16, 8"
    CornerRadius="6"
    BackgroundColor="{DynamicResource TextAndBackgroundColor_i5}"
    BorderColor="{DynamicResource TextAndBackgroundColor_i2}"
    x:Class="Next2.Controls.DatePicker"
    x:DataType="controls:DatePicker">

    <Frame.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding TapCommand, Source={RelativeSource AncestorType={x:Type controls:DatePicker}}}"/>
    </Frame.GestureRecognizers>

    <StackLayout
        Orientation="Horizontal"
        Spacing="32">

        <Label
            VerticalTextAlignment="Center"
            FontSize="{DynamicResource TSize_i6}"
            FontFamily="Barlow-Medium"
            TextColor="{DynamicResource TextAndBackgroundColor_i1}"
            Text="{Binding DateLabel, Source={RelativeSource AncestorType={x:Type controls:DatePicker}}, StringFormat={x:Static local:Constants+Formats.SHORT_DATE_FORMAT}}" />

        <ffimageloading:CachedImage Source="ic_calendar_24x24.png" />

    </StackLayout>

</Frame>