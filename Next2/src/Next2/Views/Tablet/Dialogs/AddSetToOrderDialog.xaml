<?xml version="1.0" encoding="utf-8" ?>
<rg:PopupPage
    x:Class="Next2.Views.Tablet.Dialogs.AddSetToOrderDialog"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:buttons="clr-namespace:Next2.Controls.Buttons"
    xmlns:dialogs="clr-namespace:Next2.ViewModels.Tablet.Dialogs"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:rg="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    x:DataType="dialogs:AddSetToOrderDialogViewModel">

    <Frame
        Padding="1.5"
        BackgroundColor="{DynamicResource TextAndBackgroundColor_i2}"
        CornerRadius="5"
        HeightRequest="420"
        HorizontalOptions="Center"
        VerticalOptions="Center"
        WidthRequest="360">

        <Frame.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding CloseCommand}" />
        </Frame.GestureRecognizers>

        <Frame
            Padding="20"
            BackgroundColor="{DynamicResource TextAndBackgroundColor_i2}"
            CornerRadius="5">

            <StackLayout>

                <ffimageloading:CachedImage Source="{Binding Set.ImagePath}" />

                <Label
                    Margin="0,20"
                    Text="Заглушка"
                    TextColor="{DynamicResource TextAndBackgroundColor_i1}" />

                <Label Text="{Binding Set.Title}" TextColor="{DynamicResource TextAndBackgroundColor_i1}" />

                <Label Text="{Binding Set.Price}" TextColor="{DynamicResource TextAndBackgroundColor_i1}" />

                <BoxView BackgroundColor="{DynamicResource TextAndBackgroundColor_i2}" HeightRequest="1" />

                <Grid Margin="0,10,0,0" ColumnSpacing="10">

                    <buttons:BorderButton
                        Grid.Column="0"
                        Padding="25,5"
                        BackgroundColor="{DynamicResource TextAndBackgroundColor_i2}"
                        BorderColor="{DynamicResource IndicationColor_i3}"
                        Command="{Binding CloseCommand}"
                        CornerRadius="4"
                        FontSize="{DynamicResource TSize_i8}"
                        HasShadow="False"
                        HeightRequest="40"
                        Text="{xct:Translate Cancel}" />

                    <buttons:BorderButton
                        Grid.Column="1"
                        Padding="25,5"
                        BackgroundColor="{DynamicResource IndicationColor_i2}"
                        BorderColor="{DynamicResource IndicationColor_i2}"
                        Command="{Binding CloseCommand}"
                        CornerRadius="4"
                        FontSize="{DynamicResource TSize_i8}"
                        HasShadow="False"
                        HeightRequest="40"
                        Text="{xct:Translate Add}" />

                </Grid>

            </StackLayout>

        </Frame>

    </Frame>

</rg:PopupPage>